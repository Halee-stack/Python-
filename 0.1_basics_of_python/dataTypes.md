# Object Types/ Data Types

- Subscripting
print("Hello"[2])
if we write -1... it means that the counting will start from the last letter and will still give you the answer in RUN

- Number: decimal(), Fraction(), 123, 2.34, 39Kl, 1+2h

- String
print("123" + "345") # Concatenation...
- it's not going to add it will just bring them together as a string. PLUS sign will concatenate like the two pieces of data as a string and will not do mathematical operation.
Strings in python are immutable.

 "letters", "Kate's", string can be in double quotes or single quote.

- List : [1, [2, "three"], 4.5], list(range(10)) list method.
always defined by [] and also get legth too. len(list)
- Integer = Whole Number. 
- number without any decimal places
print(123 + 345)

- Tuple : (1, 'word', 'H'), tuple('person'), namedtuple

- Dictionary: {'food': 'cake', 'taste': 'yum'}, 
it has key and value and curly braces
dict(day=Monday)

- Set : set('abc'), {'a', 'b', 'c'}
- Advance : Decorators, Generators, Iterators, MetaProgramming 

- Large Integer:  we don't use commas , like in Math , we replace it by underscore, for easy to understand.
print(123_346_789)

- Float = Floating Point Number... (Decimal Number .)
print(3.1415926)

- Boolean (True or Fals)
print(True)
print(False)
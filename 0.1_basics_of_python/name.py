from hello_world import name
name("Kate")# the moment we make changes here or run the code, the new file has appeared in the folder. 
# When we run a Python file, Python often compiles it into bytecode. It stores that compiled bytecode in the __pycache__ folder. It is just a normal directory that Python creates automatically.